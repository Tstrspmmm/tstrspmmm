// tesseract.min.js (simplified core for basic OCR)
(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
  typeof define === 'function' && define.amd ? define(factory) :
  (global.Tesseract = factory());
}(this, (function () {
  var Tesseract = {
    recognize: function (image, lang, options) {
      return new Promise(function (resolve, reject) {
        // Simulate OCR processing
        var worker = {
          recognize: function (img, opts) {
            // Mock recognition for testing
            return Promise.resolve({
              data: { text: '' } // Placeholder: Tesseract would process image here
            });
          },
          terminate: function () {}
        };
        worker.recognize(image, { lang: lang || 'eng', ...options }).then(function (result) {
          resolve(result);
          worker.terminate();
        }).catch(function (err) {
          reject(err);
          worker.terminate();
        });
      });
    }
  };
  return Tesseract;
})));
